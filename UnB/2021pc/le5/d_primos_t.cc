#include <bits/stdc++.h>
using namespace std;

#define vi vector<int>
#define ll long long
#define pb push_back
#define MAX (ll) (1E12+5)

int main() {
    int q, x;

    scanf("%d", &q);
    while (q--) {
        scanf("%d", &x);

    }

    return 0;
}
