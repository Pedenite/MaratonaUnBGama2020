#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define MAX (int) (1e6+1)
#define mod (int) (1e9+7)
#define forn(j,n) for(int i = j; i < n; i++)

int main() {
    ll a, b;

    scanf("%d %d", &a, &b);

    return 0;
}
