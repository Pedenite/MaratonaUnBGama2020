#include <bits/stdc++.h>

using namespace std;

#define vi vector<int>
#define ll long long
#define pb push_back
#define mp make_pair
#define ii pair<int,int>

int main() {
    int n, m, ai, qi;
    vi a;

    cin >> n >> m;
    while (n--) {
        cin >> ai;
        a.pb(ai);
    }

    sort(a.begin(), a.end());
    while (m--) {
        
    }

    return 0;
}
