#include <bits/stdc++.h>
using namespace std;

#define vi vector<int>
#define pb push_back

int main() {
    int n, q, ai, l, r;
    vi a;

    cin >> n >> q;
    while (n--) {
        cin >> ai;
        a.pb(ai);
    }

    return 0;
}
