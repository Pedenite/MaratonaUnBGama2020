#include <bits/stdc++.h>
using namespace std;

#define ll long long

bool div7(string n) {
    return 0LL;
}

int main() {
    int t;
    ll n;

    scanf("%d", &t);
    while (t--) {
        scanf("%lld", &n);
    }

    return 0;
}
