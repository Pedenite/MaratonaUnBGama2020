#include <bits/stdc++.h>

using namespace std;

#define vi vector<int>
#define ll long long
#define pb push_back
#define mp make_pair
#define ii pair<int,int>

int main() {
    int n, k, c, s;
    vector<ii> a;

    scanf("%d %d", &n, &k);
    while (n--) {
        scanf("%d %d", &c, &s);
        
    }

    return 0;
}
