#include <bits/stdc++.h>

using namespace std;

#define vi vector<int>
#define ll long long
#define pb push_back
#define mp make_pair
#define ii pair<int,int>

int main() {
    int n, k, aux;
    vi a;

    cin >> n >> k;
    for (int i = 0; i < n; i++) {
        cin >> aux;
        a.pb(aux);
    }

    return 0;
}
