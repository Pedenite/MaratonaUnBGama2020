#include <bits/stdc++.h>

using namespace std;

#define vi vector<int>
#define ll long long
#define pb push_back
#define mp make_pair
#define ii pair<int,int>

int main() {
    int n, x, temp;
    vi f;
    
    cin >> n >> x;
    for (int i = 0; i < n; i++) {
        cin >> temp;
        f.pb(temp);
    }

    // nao finalizada

    return 0;
}