#include <bits/stdc++.h>

using namespace std;

#define vi vector<int>
#define ll long long
#define pb push_back
#define mp make_pair
#define ii pair<int,int>

bool verifica_contato(vector<ii> contatos, int n, int a, int b) {
    for (int i = 0; i < contatos.size(); i++) {

    }

    return true;
}

int main() {
    int n, q, tipo, a, b;
    vector<pair<int,vi>> contatos;

    cin >> n >> q;
    for (int i = 0; i < q; i++) {
        cin >> tipo >> a >> b;
        if (tipo == 1) {
            // contatos.pb(mp(a, b));
        } else {

        }
    }

    return 0;
}